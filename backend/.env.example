PORT=5000
MONGODB_URI=mongodb://localhost:27017/monthly-split
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Email Configuration (for OTP verification)
# Option 1: SMTP Configuration (recommended for production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password
# EMAIL_FROM="Monthly Split" <noreply@monthlysplit.com>

# Option 2: Gmail with App Password (for development)
# GMAIL_USER=your-email@gmail.com
# GMAIL_APP_PASSWORD=your-app-password

# WhatsApp OTP Configuration (for OTP verification via mobile)
# The system uses WhatsApp deep links (wa.me) to send OTP codes
# DEFAULT_COUNTRY_CODE=+91

# Note: WhatsApp links are generated automatically. Users click the link to receive OTP via WhatsApp.
# The country code defaults to +91 (India). Change DEFAULT_COUNTRY_CODE for other countries.
# Example: For US numbers, set DEFAULT_COUNTRY_CODE=+1
